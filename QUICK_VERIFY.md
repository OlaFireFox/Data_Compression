# ⚡ 傳輸動畫系統 - 快速驗證 (5 分鐘)

## 步驟 1: 啟動系統 (1 分鐘)

### 終端 1 - 後端
```bash
cd backend
python run.py
```
✅ 看到 `Uvicorn running on http://127.0.0.1:8000`

### 終端 2 - 前端
```bash
cd frontend
python server.py
```
✅ 看到 `Serving on http://localhost:5000`

### 瀏覽器
訪問 `http://localhost:5000`

---

## 步驟 2: 測試文件上傳 (1 分鐘)

1. **創建測試文件**: 新建 `test.txt`，內容為：
   ```
   hello world hello hello test world
   ```

2. **上傳文件**
   - 拖放 `test.txt` 到拖放區域
   - ✅ 顯示檔案名稱和大小
   - ✅ 「壓縮」按鈕變為可用

---

## 步驟 3: 壓縮文件 (1 分鐘)

1. **點擊「壓縮」**
   - ✅ 進度條顯示
   - ✅ 完成後顯示統計信息
   
2. **驗證結果**
   - ✅ 「原始大小」 > 「壓縮大小」
   - ✅ 「壓縮率」 > 0%
   - ✅ 頻率直方圖顯示字符

---

## 步驟 4: 播放動畫 (2 分鐘)

1. **打開動畫**
   - 點擊「查看動畫」按鈕
   - ✅ 模態框打開
   - ✅ 顯示樹結構
   - ✅ SVG 傳輸區域可見（兩條水平線）

2. **播放動畫**
   - 點擊「▶️ 播放」
   - ✅ **關鍵**: 觀察兩個圓圈向右移動
     - 🔵 **左側** (藍色，上方) = 原始 8-bit 傳輸 → 完整移動距離
     - 🟣 **右側** (紫色，下方) = Huffman 壓縮傳輸 → 較短移動距離 (通常)
   - ✅ 狀態文本框實時更新

3. **驗證狀態文本**
   - 📍 Line 1: 顯示字符 ('h', 'e', 'l', 'o', 'w', 'd' 等)
   - 🔤 Line 2: 顯示頻率 (數字)
   - 💾 Line 3: 顯示二進制編碼
   - 📏 Line 4: 顯示位數 (比如 "3 bits vs 8 bits")
   - ⚡ Line 5: 顯示時長對比

---

## 步驟 5: 測試速度控制 (快速驗證)

1. **調整速度滑塊**
   - 移到「0.5x」: 動畫變慢 ✅
   - 移到「2x」: 動畫變快 ✅

2. **重放動畫**
   - 重置並以 1x 速度再次播放 ✅

---

## ✅ 成功標誌

系統正常運作的跡象：

1. **文件上傳**
   - ✅ 能選擇和上傳 .txt 文件
   
2. **壓縮**
   - ✅ 壓縮率 > 10% (通常 20-50%)
   - ✅ 編碼表有多個條目

3. **動畫 (最重要)**
   - ✅ **兩個圓圈均移動** (不是只有一個)
   - ✅ **Huffman 圓圈(紫色)比原始圓圈(藍色)移動距離短**
   - ✅ **狀態文本實時更新**
   - ✅ **動畫流暢 (無卡頓)**

---

## ❌ 常見問題

### 問題: 動畫不顯示
**檢查**:
1. 打開開發者工具 (F12) → 控制台
2. 查看是否有紅色錯誤訊息
3. 驗證 GSAP 已加載: 在控制台輸入 `window.gsap`
4. 驗證 TransmissionAnimator 已加載: 輸入 `window.TransmissionAnimator`

### 問題: 只有藍色圓圈移動
**原因**: Huffman 動畫器未初始化
**解決**: 確保壓縮結果包含 `code_table` 和 `frequencies`

### 問題: 狀態文本不更新
**原因**: 元素 ID 不匹配或 DOM 未就緒
**解決**: 檢查 index.html 中 statusLine1-5 的 ID

### 問題: 後端連接失敗
**檢查**:
1. 後端是否運行在 `http://127.0.0.1:8000`
2. CORS 是否已啟用
3. 控制台是否有網絡錯誤

---

## 📊 數據流

```
用戶 → 上傳文件
   ↓
後端 → Huffman 壓縮
   ↓ (返回 code_table, frequencies, build_steps)
前端 → 顯示結果
   ↓
用戶 → 點擊「查看動畫」
   ↓
TransmissionAnimator → 初始化
   ↓
GSAP Timeline → 並行動畫
   ↓ (每個字符)
原始軌道: 8 bits × 0.25s = 2秒
Huffman軌道: code.length × (1/speed)秒
   ↓
實時更新狀態文本
```

---

## 🎯 預期行為示例

對於文件 "hello"，frequency: h=1, e=1, l=2, o=1

### 字符 'l' (頻率=2, 編碼='0' 長度=1)
- 📍 L1: "正在處理字符: 'l'"
- 🔤 L2: "頻率: 2 次"
- 💾 L3: "編碼: 0"
- 📏 L4: "長度: 1 bits (原始: 8 bits)"
- ⚡ L5: "時長: Huffman 1.00s vs 原始 2.00s"

**動畫視覺**:
- 藍色圓圈移動 410px (固定)
- 紫色圓圈移動 ~51px (1/8 距離)

---

## 下一步

✅ 所有檢查通過?
→ 查看 [TESTING_GUIDE.md](TESTING_GUIDE.md) 進行完整測試

❌ 有問題?
→ 檢查開發者工具控制台的錯誤訊息
→ 查看 [README.md](README.md) 的故障排除部分
