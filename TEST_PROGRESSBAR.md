# 進度條竞速動畫測試說明

## 功能概述

已將傳輸速度對比改為「進度條竞速」模式。

### 視覺呈現
- **上方進度條**：原始 8-bit 傳輸，恆定速度
- **下方進度條**：Huffman 壓縮傳輸，速度根據編碼長度動態變化
- 兩條進度條從 0% 同時開始，各自向 100% 移動

### 動畫邏輯

**時間計算公式**：
```
基礎時間 T = 0.8s（代表 8-bit 傳輸時間）
每個 bit 傳輸時間 = 0.1s

原始進度條時長 = 8 × 0.1s = 0.8s（固定）
Huffman進度條時長 = huffman_bits × 0.1s（動態）
```

**進度增加量**：
```
每處理一個字符：增加 1 / 總字符數 的進度
例如：39 個字符，每步增加 1/39 ≈ 2.56% 的進度
```

## 測試步驟

### 1. 準備測試文件
建立文件 `test.txt`，內容如下：
```
aaaaaabbbc
```
（或使用更長的文本來更明顯地看到速度差異）

### 2. 執行測試
1. 刷新瀏覽器 (Ctrl+Shift+R)
2. 上傳測試文件
3. 點擊「壓縮」按鈕
4. 點擊「查看動畫」打開動畫模態
5. 點擊「▶️ 播放」開始動畫

### 3. 觀察現象

#### 預期行為 ✅

- **開始時**：兩條進度條都在 0%
- **處理 'a' 字符** (編碼很短，如 1-2 bits)：
  - 原始進度條：以恆定速度填充
  - **Huffman 進度條會瞬間衝到終點**（因為只需 0.1-0.2s）
  - 視覺上看到下方進度條「瘋狂衝刺」
  
- **處理 'b' 或 'c' 字符** (編碼較長，如 3-4 bits)：
  - Huffman 進度條速度變慢（但仍快於原始進度條）
  - 兩條進度條的速度差異更明顯

- **最後**：兩條進度條都到達 100%

#### 數字驗證

檢查控制台日誌（F12），應該看到：
```
📍 字符: 'a' (1/10) | 編碼: 0 (1 bits)
📊 進度: 0% → 10% | 寬度增加: 10%
⏱️ 時長: 原始 0.80s vs Huffman 0.10s | 節省 87.5%
🎬 開始進度條竞速... (原始: 0.80s, Huffman: 0.10s)
```

這表明：
- 'a' 的編碼只有 1 bit
- Huffman 版本比原始版本快 8 倍 (0.8 / 0.1)
- 可節省 87.5% 的傳輸時間

### 4. 測試速度調節

在播放過程中：
1. 調整「速度」滑條到 2x
   - 兩條進度條都應加快（但保持速度關係）
2. 調整到 0.5x
   - 兩條進度條都應減速
3. 應該看到控制台日誌：`⚡ 速度調整: 2x`

### 5. 測試暫停/恢復

1. 點擊「▶️ 播放」開始
2. 播放幾秒後點擊「⏸️ 暫停」
   - 進度條應該停止
   - 按鈕文字應變為「▶️ 重新開始」
3. 再次點擊「▶️ 重新開始」
   - 應重新開始從 0% 播放

## 故障排查

| 問題 | 解決方案 |
|------|--------|
| 進度條沒有移動 | 檢查瀏覽器控制台是否有錯誤。確認 SVG 元素存在 |
| 進度文字不更新 | 這是已知的限制；GSAP 的 textContent 動畫支持有限。改用 onUpdate 回調手動更新 |
| 速度調節無效 | 確認已點擊「播放」按鈕；只有播放中才能調節速度 |
| 動畫很慢/很快 | 檢查「速度」滑條值；確認沒有設置為 0.5x 以下 |

## 預期結果

✅ 進度條竞速模式正常運作
✅ Huffman 進度條明顯快於原始進度條
✅ 進度百分比正確顯示
✅ 速度調節即時生效
✅ 樹節點高亮與進度條同步

---

測試完成後，請反饋：
- 進度條是否正確移動？
- 速度差異是否明顯？
- 是否有 UI 或動畫問題？
