# ✅ 智能路徑動畫 - 驗證清單

使用此清單驗證重構後的動畫邏輯是否正常運作。

---

## 🧪 預備階段

- [ ] 後端運行：`python -m uvicorn main:app --reload --port 8000`
- [ ] 前端運行：`python -m http.server 3000`
- [ ] 瀏覽器打開：`http://localhost:3000`
- [ ] DevTools 打開：F12 → 控制台

---

## 📝 測試用例

### 測試 1: 基礎遞進動畫

**文件內容**：`a`（單個字符）

**預期結果**：
- [ ] 樹上看到根節點逐步亮起
- [ ] 然後是中間節點
- [ ] 最後是葉節點 'a'
- [ ] 控制台輸出：
  ```
  🔄 路徑變化 - 字符 'a':
     舊路徑: (無)
     新路徑: 1 → 2 → 5
     ✨ 步驟 1/3 - 高亮節點: 1
     ✨ 步驟 2/3 - 高亮節點: 1 → 2
     ✨ 步驟 3/3 - 高亮節點: 1 → 2 → 5
     ✓ 遞進式高亮完成
  ```

---

### 測試 2: 脈衝閃爍 (相同字符)

**文件內容**：`aa`（連續相同字符）

**預期結果**：
- [ ] 第 1 個 'a'：遞進式亮起 (300ms)
- [ ] 第 2 個 'a'：**快速脈衝** (相同路徑)
  - 看到 3 次快速閃爍
  - 然後路徑保持亮起
- [ ] 控制台輸出：
  ```
  【第 1 個 'a'】
  🔄 路徑變化 - 字符 'a':
     ✨ 步驟 1/3 ...
     ✨ 步驟 2/3 ...
     ✨ 步驟 3/3 ...
     ✓ 遞進式高亮完成

  【第 2 個 'a'】
  🔁 路徑相同 - 字符 'a' (連續字符):
     💫 脈衝閃爍動畫 (1x 速度 | 300ms):
        💥 脈衝 1/3
        💥 脈衝 2/3
        💥 脈衝 3/3
        ✓ 脈衝閃爍完成
  ```

---

### 測試 3: 路徑切換

**文件內容**：`ab`（路徑不同的字符）

**預期結果**：
- [ ] 第 1 個 'a'：遞進式亮起 (路徑 A)
- [ ] 第 2 個 'b'：
  - 先看到所有高亮**清除** (50ms)
  - 然後新路徑遞進式亮起 (路徑 B)
  - 路徑 A 和路徑 B 不同
- [ ] 控制台輸出：
  ```
  【第 1 個 'a'】
  🔄 路徑變化 - 字符 'a':
     舊路徑: (無)
     新路徑: 1 → 2 → 5
     ✓ 遞進式高亮完成

  【第 2 個 'b'】
  🔄 路徑變化 - 字符 'b':
     舊路徑: 1 → 2 → 5
     新路徑: 1 → 3 → 4
     ✓ 遞進式高亮完成
  ```

---

### 測試 4: 複雜序列

**文件內容**：`aaabbc`（最全面的測試）

**預期結果**：

| 字符 | 效果 | 說明 |
|------|------|------|
| 1: 'a' | 🔄 遞進 | 首次出現路徑 [1→2→5] |
| 2: 'a' | 💫 脈衝 | 相同路徑，快速閃爍 |
| 3: 'a' | 💫 脈衝 | 相同路徑，快速閃爍 |
| 4: 'b' | 🔄 遞進 | 新路徑 [1→3→4]，清除+亮起 |
| 5: 'b' | 💫 脈衝 | 相同路徑，快速閃爍 |
| 6: 'c' | 🔄 遞進 | 新路徑 [1→...→...]，清除+亮起 |

- [ ] 看到 3 種不同的動畫效果
- [ ] 進度條與樹動畫同步運行
- [ ] 控制台顯示完整的日誌

---

## ⚙️ 速度測試

### 速度 1.0x (基準)

**操作**：
1. 設置速度滑塊為 1.0x
2. 播放 `aaabbc`
3. 觀察動畫時間

**驗證**：
- [ ] 遞進式高亮：~300ms (每步 100ms)
- [ ] 脈衝閃爍：~300ms (3 次脈衝)
- [ ] 進度條動畫：~0.8s (8-bit 標準時間)

**控制台檢查**：
```
📍 遞進式高亮路徑 (3 節點 | 1x 速度):
```

---

### 速度 2.0x (快速)

**操作**：
1. 設置速度滑塊為 2.0x
2. 播放 `aaabbc`
3. 觀察動畫時間

**驗證**：
- [ ] 遞進式高亮：~150ms (**一半時間**，每步 50ms)
- [ ] 脈衝閃爍：~150ms (**一半時間**，脈衝更快)
- [ ] 進度條動畫：~0.4s (**加倍快速**)

**控制台檢查**：
```
📍 遞進式高亮路徑 (3 節點 | 2x 速度):
```

---

### 速度 0.5x (緩慢)

**操作**：
1. 設置速度滑塊為 0.5x
2. 播放 `aaabbc`
3. 觀察動畫時間

**驗證**：
- [ ] 遞進式高亮：~600ms (**2 倍時間**，每步 200ms)
- [ ] 脈衝閃爍：~600ms (**2 倍時間**，脈衝更慢)
- [ ] 進度條動畫：~1.6s (**減速**)

**控制台檢查**：
```
📍 遞進式高亮路徑 (3 節點 | 0.5x 速度):
```

---

## 🐛 故障檢查

### 問題 1: 沒有看到遞進式高亮

**診斷步驟**：
```javascript
// F12 控制台執行
appState.treeVisualizer.highlightPathProgressive([1, 2, 5], 1.0)
```

- [ ] 有 3 步逐次出現嗎？
- [ ] Canvas 上看到節點亮起嗎？
- [ ] 控制台有日誌輸出嗎？

**如果都是 ❌**：
- [ ] 檢查樹數據是否加載：`console.log(appState.treeVisualizer.treeData)`
- [ ] 檢查 Canvas 是否可見
- [ ] 刷新頁面並重試

---

### 問題 2: 沒有看到脈衝閃爍

**診斷步驟**：
```javascript
// F12 控制台執行
await appState.treeVisualizer.pulsePathFlash([1, 2, 5], 1.0)
```

- [ ] 看到快速的亮/暗交替嗎？
- [ ] 動畫後路徑保持亮起嗎？
- [ ] 控制台有「💥 脈衝」日誌嗎？

**如果都是 ❌**：
- [ ] 檢查 clearAllHighlights() 是否工作：`appState.treeVisualizer.clearAllHighlights()`
- [ ] 檢查 drawTree(nodePath) 是否工作：`appState.treeVisualizer.drawTree([1, 2, 5])`
- [ ] 嘗試手動調整 pulseInterval：編輯 visualization.js 第 82 行

---

### 問題 3: 路徑對比不工作

**診斷步驟**：
```javascript
// F12 控制台執行
const path1 = [1, 2, 5];
const path2 = [1, 2, 5];
console.log(appState.treeVisualizer.arePathsEqual(path1, path2))
// 應返回 true
```

**如果返回 false**：
- [ ] 檢查路徑內容：`console.log(path1, path2)`
- [ ] 檢查陣列長度：`path1.length === path2.length`

---

### 問題 4: 速度調整無效

**診斷步驟**：
```javascript
// F12 控制台執行
console.log('當前速度:', appState.animationSpeed)
// 應該顯示滑塊對應的值
```

**如果顯示 1（固定）**：
- [ ] 檢查 main.js 第 585 行：`appState.animationSpeed = parseFloat(elements.animationSpeed.value)`
- [ ] 確保速度滑塊的 ID 是 `animationSpeed`
- [ ] 檢查 HTML 中滑塊元素是否存在

---

## 📊 效能檢查

### CPU 使用率

**使用 Chrome DevTools Performance**：

1. 打開 DevTools → Performance
2. 點擊紅色「記錄」按鈕
3. 播放動畫
4. 停止記錄

**預期**：
- [ ] 幀率穩定在 ~60fps
- [ ] 沒有長時間的掉幀 (yellow/red 條)
- [ ] Canvas 重繪時間 < 16ms

### 內存使用

**使用 Chrome DevTools Memory**：

1. 打開 DevTools → Memory
2. 拍攝初始快照
3. 播放動畫多次
4. 拍攝結束快照

**預期**：
- [ ] 內存增長 < 10MB
- [ ] 播放後沒有內存洩漏
- [ ] 停止播放後內存回到基準

---

## ✨ 視覺驗證清單

### 第 1 個字符 (首次路徑)

播放文件 `aaabbc`，觀察第 1 個 'a'：

- [ ] **清晰看到節點逐步亮起**
  - 根節點先亮 (黃色 #ffff00)
  - 然後中間節點 (黃色 #ffff00)
  - 最後葉節點 (黃色 #ffff00)
- [ ] 邊 (連線) 也逐步亮起
- [ ] 過程自然流暢 (~300ms)

### 第 2-3 個字符 (相同路徑)

觀察第 2-3 個 'a'：

- [ ] **看到快速閃爍** (相對於遞進式)
  - 整條路徑一起「閃爍」(亮→暗→亮)
  - 3 次脈衝
- [ ] 閃爍後路徑保持亮起
- [ ] 與第 1 個 'a' 的效果**明顯不同**

### 第 4 個字符 (路徑改變)

觀察第 4 個 'b'：

- [ ] **看到路徑清除**
  - 第 3 個 'a' 的路徑短暫消失
- [ ] **看到新路徑遞進式亮起**
  - 新的根→中→葉
  - 與第 1 個 'a' 的效果相同，但路徑不同

### 進度條同步

- [ ] 路徑動畫完成後，進度條立即開始移動
- [ ] 沒有明顯的延遲或卡頓
- [ ] 原始/Huffman 進度條同步運行

---

## 🔊 控制台日誌驗證

### 完整播放的日誌應該包含：

```
═══════════════════════════════════════════════════
▶️ 開始播放動畫 - 智能路徑對比模式
═══════════════════════════════════════════════════

【字符 1】
📍 字符 1/6: 'a'
   🌳 顯示樹構建步驟 ...
   🧠 執行路徑對比邏輯...
🔄 路徑變化 - 字符 'a':
   舊路徑: (無)
   新路徑: 1 → 2 → 5
   📍 遞進式高亮路徑 (3 節點 | 1x 速度):
      ✨ 步驟 1/3 - 高亮節點: 1
      ✨ 步驟 2/3 - 高亮節點: 1 → 2
      ✨ 步驟 3/3 - 高亮節點: 1 → 2 → 5
      ✓ 遞進式高亮完成
📍 字符: 'a' | 編碼: ... (... bits)
...進度條動畫日誌...

【字符 2】
📍 字符 2/6: 'a'
   🌳 顯示樹構建步驟 ...
   🧠 執行路徑對比邏輯...
🔁 路徑相同 - 字符 'a' (連續字符):
   路徑: 1 → 2 → 5
   💫 脈衝閃爍動畫 (1x 速度 | 300ms):
      💥 脈衝 1/3
      💥 脈衝 2/3
      💥 脈衝 3/3
      ✓ 脈衝閃爍完成
...

✅ 所有字符傳輸完成 - 動畫播放結束
═══════════════════════════════════════════════════
```

**檢查清單**：
- [ ] 有「🔄 路徑變化」輸出（遞進式高亮）
- [ ] 有「🔁 路徑相同」輸出（脈衝閃爍）
- [ ] 有「💫 脈衝閃爍動畫」和「💥 脈衝」輸出
- [ ] 有「✨ 步驟 X/X」輸出（遞進步驟）
- [ ] 末尾有「✅ 所有字符傳輸完成」

---

## 🎯 最終驗收標準

播放完整文件 `aaabbc` 後，確認以下全部 ✅：

**視覺效果**：
- [ ] 遞進式高亮清晰 (首次字符)
- [ ] 脈衝閃爍明顯 (重複字符)
- [ ] 路徑切換流暢 (不同字符)

**時間同步**：
- [ ] 樹動畫和進度條無延遲
- [ ] 速度滑塊改變動畫速度
- [ ] 暫停/恢復正常工作

**日誌完整**：
- [ ] 控制台日誌清晰且完整
- [ ] 沒有錯誤或警告
- [ ] 時間計算正確

**性能穩定**：
- [ ] 幀率穩定 (60fps)
- [ ] 沒有卡頓
- [ ] 內存使用正常

---

## 📞 回報問題

如果以上檢查發現問題，請記錄：

1. **問題描述**：看到什麼錯誤？
2. **重現步驟**：如何重現問題？
3. **控制台日誌**：複製完整的日誌
4. **瀏覽器/OS**：使用什麼瀏覽器和作業系統？
5. **截圖/錄影**：附加視覺證據

---

**驗證完成日期**：____________

**驗證者簽名**：____________

✅ **新邏輯已驗收！**

